

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ascendai-career-buddy
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - DATABASE_URL=${DATABASE_URL_DOCKER}
    restart: always
